plugins {
    id 'java-platform'
}

javaPlatform {
    allowDependencies()
}

dependencies {
    api platform(libs.spring.boot.dependencies)

    constraints {
        // Add version constraints for non-Spring dependencies here
        // api 'com.google.guava:guava:33.4.0-jre'
    }
}
